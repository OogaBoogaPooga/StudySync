# ---- Required ----
# SQLite file location (relative to prisma/ folder). No DB server needed.
DATABASE_URL="file:./dev.db"

# Secret used to sign JWT tokens. Use a long random string in production:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="change-me-to-a-long-random-string"

# ---- Optional ----
# Port for the Express API (Vite dev server proxies to this)
PORT=4000

# OpenAI-compatible API key for AI flashcard generation.
# Leave blank to use the built-in offline heuristic generator.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"

# Set to "production" when deploying so Express serves the built client
NODE_ENV=development
